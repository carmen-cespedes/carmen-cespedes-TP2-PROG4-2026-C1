import { DeshabilitarSi } from './deshabilitar-si';

describe('DeshabilitarSi', () => {
  it('should create an instance', () => {
    const directive = new DeshabilitarSi();
    expect(directive).toBeTruthy();
  });
});
