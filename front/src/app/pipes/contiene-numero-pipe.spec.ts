import { ContieneNumeroPipe } from './contiene-numero-pipe';

describe('ContieneNumeroPipe', () => {
  it('create an instance', () => {
    const pipe = new ContieneNumeroPipe();
    expect(pipe).toBeTruthy();
  });
});
